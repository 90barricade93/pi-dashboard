/**
 * Cross-component timing constants.
 * Storage-related constants live alongside their helpers in twitter-client-storage.ts.
 */

/** How often the (mock) network stats widget refreshes. */
export const NETWORK_STATS_POLL_INTERVAL_MS = 30 * 1000;
